!!!
%html
  %head
    %title cojiro
    = stylesheet_link_tag :application
    = javascript_include_tag :application
    = csrf_meta_tags
  %body{ :style => "padding-top: 40px" }
    - flash.each do |name, msg|
      = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          = link_to "cojiro", homepage_path, :class => "brand"
          %ul.nav
            - if logged_in?
              %li= link_to @current_user.name, "#"
              %li= link_to "Logout", logout_path
            - else
              %li= link_to "Sign in through Twitter", '/auth/twitter'
    .container
      .content
        - flash.each do |name, msg|
          = content_tag :div, '<a class="close" href="#">×</a>'.html_safe + msg, :class => "alert-message #{name}", :id => "flash_#{name}", :'data-alert' => 'alert' if msg.is_a?(String)
        = yield
