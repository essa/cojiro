-if (current = @model.get(@attr_name))
  %span.translated{ :class => @editable_field_class_name }=current
-else
  %span.untranslated{ :class => @editable_field_class_name }=@model.getAttrInSourceLocale(@attr_name)
